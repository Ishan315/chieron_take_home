# ClinicalTrials Query-to-Visualization Agent Environment Configuration

# Optional API Keys (System includes a deterministic rule-based NLP fallback engine if left empty)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

GEMINI_API_KEY=

# ClinicalTrials.gov API v2 Base URL
CLINICAL_TRIALS_API_BASE=https://clinicaltrials.gov/api/v2

# Default max trials to analyze per request
DEFAULT_MAX_TRIALS=200
